class PollableSubscribeOne {
  constructor(subscription) {
    // throw new Error(`TODO: PollableSubscribeOne`);
  }
}

class PollableInterval {
  constructor(milliseconds) {
    this.milliseconds = milliseconds;
    // throw new Error(`TODO: PollableInterval`);
  }
}

async function PerformPoll(devices, timeoutMs) {
  throw new Error(`TODO: PerformPoll`);
}

module.exports = {
  PollableSubscribeOne,
  PollableInterval,
  PerformPoll,
};
